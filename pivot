select
	Gender
	,[Sales Representative]
	,[Buyer]
	,[Janitor]
	
from
(
select 
	Gender
	,JobTitle
	,VacationHours
from HumanResources.Employee
) a
pivot(
	avg(VacationHours)
	for JobTitle in([Buyer],[Janitor],[Sales Representative])
) p

